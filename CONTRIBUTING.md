# Contributing

Install the locked toolchain and run the complete contract suite before opening a pull request:

```bash
npm ci
npm run check
```

The tests run a real isolated OpenCode server against a fake Graphify executable. Keep first indexing human-gated, background work non-blocking, and every Graphify subprocess bounded and recoverable.
